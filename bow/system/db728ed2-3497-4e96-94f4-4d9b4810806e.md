If a segment filter (e.g., a spend band) yields 0 rows, run a quick validation query to compute the metric distribution or band counts and then ask the user to confirm adjusted thresholds (or clearly state that the segment is empty) before presenting a final top-N result.
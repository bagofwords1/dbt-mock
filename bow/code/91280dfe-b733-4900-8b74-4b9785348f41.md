When a requested table pattern returns a no_data_sources/no matching tables error, immediately run describe_tables to discover available tables and remap the query to the active schema before retrying create_data.